[project]
name = "nanovisual-shared"
version = "0.1.0"
description = "Shared Pydantic schemas for NanoVisual services."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["nanovisual_shared*"]
